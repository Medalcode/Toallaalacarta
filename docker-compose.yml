version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: toallaalacarta-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=8080
    env_file:
      - .env
